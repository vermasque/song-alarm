preconditions
	preferences set to defaults (none, press to select)
	alarm enable box not checked
	open time dialog
		should have defaults set	
	click cancel button

neither not set
	try to enable alarm

song not set
	set time
		click time preference 
		click ok on time picker dialog
	try to enable alarm

time not set
	set song
		click on song preference
		click any track from any source
		click ok
	try to enable alarm

successful case
	set time
	set song
	enable alarm for a minute from now
	check that alarm is still enabled (repeats by default)

song duration string formatting
	test double digit minute and second
	test single digit minute and second

change time
	set song 
	set time to 5 minutes from now
	open dialog to set time
		check that time in dialog matches previously previously set time
		change time to 3 minutes from now
	enable alarm
	assert alarm is on
	change time to one minute from now
	assert alarm is on
	assert alarm goes off in the next minute

state transition (use package browser in emulator to restart activity)
	enable the alarm for arbitrary song and time one minute from now
	restart activity
	should see all song and alarm time as previously set
	alarm should go	off 

day before test
	set valid song
	set time to one minute before now
	enable alarm
	alarm should be on but not play song immediately

DEFER - post issues to Github after testing on real phone

time zone change
24-hr clock support
logcat messages
external storage state
